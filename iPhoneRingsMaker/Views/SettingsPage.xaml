<Page
    x:Class="iPhoneRingsMaker.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:helpers="using:iPhoneRingsMaker.Helpers"
    xmlns:xaml="using:Microsoft.UI.Xaml">
    <Page.Resources>
        <helpers:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
    </Page.Resources>

    <ScrollViewer>
        <Grid MaxWidth="900" Padding="24" RowSpacing="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                x:Uid="Settings_Title"
                AutomationProperties.AutomationId="SettingsPageTitle"
                Style="{StaticResource PageTitleStyle}" />

            <Border
                Grid.Row="1"
                Padding="20"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="{ThemeResource ControlCornerRadius}">
                <StackPanel Spacing="12">
                    <TextBlock x:Uid="Settings_Personalization" Style="{ThemeResource SubtitleTextBlockStyle}" />
                    <TextBlock x:Uid="Settings_Theme" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <RadioButtons AutomationProperties.AutomationId="ThemeSelector">
                        <RadioButton
                            x:Uid="Settings_Theme_Light"
                            AutomationProperties.AutomationId="LightThemeOption"
                            Command="{x:Bind ViewModel.SwitchThemeCommand}"
                            GroupName="AppTheme"
                            IsChecked="{x:Bind ViewModel.ElementTheme, Mode=OneWay, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Light}">
                            <RadioButton.CommandParameter>
                                <xaml:ElementTheme>Light</xaml:ElementTheme>
                            </RadioButton.CommandParameter>
                        </RadioButton>
                        <RadioButton
                            x:Uid="Settings_Theme_Dark"
                            AutomationProperties.AutomationId="DarkThemeOption"
                            Command="{x:Bind ViewModel.SwitchThemeCommand}"
                            GroupName="AppTheme"
                            IsChecked="{x:Bind ViewModel.ElementTheme, Mode=OneWay, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Dark}">
                            <RadioButton.CommandParameter>
                                <xaml:ElementTheme>Dark</xaml:ElementTheme>
                            </RadioButton.CommandParameter>
                        </RadioButton>
                        <RadioButton
                            x:Uid="Settings_Theme_Default"
                            AutomationProperties.AutomationId="SystemThemeOption"
                            Command="{x:Bind ViewModel.SwitchThemeCommand}"
                            GroupName="AppTheme"
                            IsChecked="{x:Bind ViewModel.ElementTheme, Mode=OneWay, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Default}">
                            <RadioButton.CommandParameter>
                                <xaml:ElementTheme>Default</xaml:ElementTheme>
                            </RadioButton.CommandParameter>
                        </RadioButton>
                    </RadioButtons>
                </StackPanel>
            </Border>

            <Border
                Grid.Row="2"
                Padding="20"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="{ThemeResource ControlCornerRadius}">
                <StackPanel Spacing="12">
                    <TextBlock x:Uid="Settings_Playback" Style="{ThemeResource SubtitleTextBlockStyle}" />
                    <NumberBox
                        x:Uid="Settings_SkipInterval"
                        Width="180"
                        HorizontalAlignment="Left"
                        AutomationProperties.AutomationId="PlaybackSkipIntervalNumberBox"
                        LargeChange="5"
                        Maximum="10"
                        Minimum="1"
                        SmallChange="1"
                        SpinButtonPlacementMode="Inline"
                        ValidationMode="InvalidInputOverwritten"
                        Value="{x:Bind ViewModel.SkipIntervalSeconds, Mode=OneWay}"
                        ValueChanged="SkipIntervalNumberBox_ValueChanged" />
                </StackPanel>
            </Border>

            <Border
                Grid.Row="3"
                Padding="20"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="{ThemeResource ControlCornerRadius}">
                <StackPanel Spacing="8">
                    <TextBlock x:Uid="Settings_About" Style="{ThemeResource SubtitleTextBlockStyle}" />
                    <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}" Text="{x:Bind ViewModel.VersionDescription, Mode=OneWay}" />
                    <TextBlock x:Uid="Settings_AboutDescription" Style="{ThemeResource BodyTextBlockStyle}" TextWrapping="Wrap" />
                    <HyperlinkButton
                        x:Uid="SettingsPage_PrivacyTermsLink"
                        AutomationProperties.AutomationId="PrivacyPolicyLink" />
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
